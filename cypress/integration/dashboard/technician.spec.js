/// <reference types="cypress" />

context('Technician Dashboard', () => {
  // beforeEach(() => {
  //   cy.login('testUser@mail.com')
  // })
  // it('Displays the user role as "technician" in the user dropdown', () => {
  //   // cy.get('[data-cy=usermenubutton]').click()
  //   // cy.get('[data-cy=usermenu]').should('contain', 'role: technician')
  // })
})
